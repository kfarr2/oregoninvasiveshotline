language: python
python:
  - "3.3"
sudo: false
# command to install dependencies
install:
  - pip install -r requirements.txt
    # command to run tests
script:
  - LOCAL_SETTINGS_FILE="local.cfg#test" ./manage.py test
services:
  - elasticsearch
before_script:
  - sleep 10
